[Unit]
Description=MinusculeClaw Daily Heartbeat

[Service]
Type=oneshot
WorkingDirectory=%h/minusculeclaw
EnvironmentFile=%h/minusculeclaw/.env
ExecStart=/bin/bash -lc 'cd %h/minusculeclaw && ./heartbeat.sh'
